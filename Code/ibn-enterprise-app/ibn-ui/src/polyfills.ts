/**
 * Polyfills required by Angular.
 *
 * Zone.js is required by default for Angular's change detection.
 * Importing it here ensures the global Zone is available before the app bootstraps.
 */
import 'zone.js/dist/zone'; // Included with Angular CLI.
